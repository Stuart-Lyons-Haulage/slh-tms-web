import { useCallback, useEffect, useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { request, type LoadDispatch, type Trailer, type Vehicle } from "../lib/api";
import { useAccessToken } from "../lib/auth";
import { getDriverDispatchRoute, getRunDispatch } from "../api/runs";
import { firstCollectionStop, runDirection, suggestionRunLabel } from "./DriverDispatchPlanning";
import "../driver-dispatch.css";
import "../driver-dispatch-compact.css";

type DispatchStop = { id: string; sequence: number; name: string; address?: string; latitude?: number; longitude?: number; plannedArrivalUtc?: string; plannerNote?: string };
type DispatchLoad = { id: string; reference: string; rawReference: string; planningDate: string; status: string; driverId?: string; vehicleId?: string; trailerId?: string; palletSpacesUsed?: number; totalPalletSpaces?: number; capacityType?: string; plannerNotes?: string; southbound: boolean; plannedStartUtc?: string; stops: DispatchStop[] };
type DispatchDriver = { driverId: string; employeeNumber: string; displayName: string; driverType: "Employed" | "Casual" | "Agency"; driverGroup?: string; skills?: string; coding?: string; agencyName?: string; tachoMasterDriverId?: string; tachoCardNumber?: string; dayNumber: number; onLeave: boolean; leaveType?: string; partDayLeave: boolean; previousRunReference?: string; previousVehicleId?: string; previousVehicleRegistration?: string; previousFinalStop?: string; previousRoute?: string; assignedLoadId?: string; assignedRunCount: number; suggestedRunId?: string; suggestedRunReference?: string; suggestedVehicleId?: string; suggestedVehicleRegistration?: string; assistantScore?: number; suggestion?: string; agencyBookedFrom?: string; agencyBookedThrough?: string };
type Workbench = { planningDate: string; weekStart?: string; weekEnd?: string; leaveSource: string; assistantSource?: string; drivers: DispatchDriver[]; vehicles: Vehicle[]; trailers: Trailer[]; loads: DispatchLoad[] };
type DriverType = DispatchDriver["driverType"];
type DriverForm = { displayName: string; employeeNumber: string; driverType: DriverType; agencyName: string; startDate: string; days: number };
type DispatchStatus = "No Run" | "Awaiting Dispatch" | "Sent Awaiting Response" | "Confirmed";
type DriverDispatchStatus = { driverId: string; dispatchStatus: DispatchStatus; lastDriverReply?: string; lastDriverReplyAtUtc?: string; lastDispatchSentAtUtc?: string; weeklyRestStatus: "Ready" | "DueSoon" | "Overdue" | "Unverified" | "Unknown"; weeklyRestMessage: string; weeklyRestDueUtc?: string; lastWeeklyRestEndUtc?: string };
type MessageMode = "initial" | "amendment";
type MessageState = { load: DispatchLoad; text: string; routeMinutes: number; acknowledgeUnverified: boolean; mode: MessageMode };
type DispatchReadiness = { canDispatch: boolean; explanation?: string; structuralReadiness?: { classification: "Recommended" | "Unverified" | "Blocked"; requiresAcknowledgement: boolean; checks: Array<{ passed: boolean; message: string }> } };
type FilterKey = "driver" | "typeSkills" | "code" | "day" | "previous" | "vehicle" | "trailer" | "run" | "assistant" | "dispatch";
type Filters = Record<FilterKey, string>;
type SearchOption = { id: string; label: string; search?: string };

const filterKeys: FilterKey[] = ["driver", "typeSkills", "code", "day", "previous", "vehicle", "trailer", "run", "assistant", "dispatch"];
const filterPlaceholders: Record<FilterKey, string> = { driver: "Driver…", typeSkills: "Type / skill…", code: "Code…", day: "Day…", previous: "Previous…", vehicle: "Vehicle…", trailer: "Trailer…", run: "Run…", assistant: "Assistant…", dispatch: "Status…" };
function emptyFilters(): Filters { return Object.fromEntries(filterKeys.map(key => [key, ""])) as Filters; }
function isoDate(date: Date) { return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`; }
function today() { return isoDate(new Date()); }
function localTime(value?: string) { if (!value) return ""; const date = new Date(value); return Number.isNaN(date.getTime()) ? "" : date.toLocaleTimeString("en-GB", { hour: "2-digit", minute: "2-digit", hourCycle: "h23" }); }
function orderedStops(load?: DispatchLoad) { return [...(load?.stops || [])].sort((a, b) => a.sequence - b.sequence); }
function runNumber(load?: DispatchLoad) { if (!load) return "—"; const match = `${load.reference} ${load.rawReference}`.match(/\b(?:run\s*)?(\d{1,3})\b/i); return match?.[1] || load.reference; }
function compactRun(load?: DispatchLoad) { return load ? `RUN ${runNumber(load)}` : "—"; }
function cleanStopName(value?: string) { return (value || "").replace(/^(?:Collect|Deliver)\s*[·:-]\s*/i, "").replace(/-/g, " ").trim(); }
function finalDestination(load?: DispatchLoad) { const stops = orderedStops(load); return cleanStopName(stops.at(-1)?.name) || "Destination not set"; }
function runPeriod(load?: DispatchLoad) { const first = firstCollectionStop(load); const value = first?.plannedArrivalUtc || load?.plannedStartUtc; if (!value) return "AM/PM"; const date = new Date(value); if (Number.isNaN(date.getTime())) return "AM/PM"; return date.getHours() < 12 ? "AM" : "PM"; }
function runSummary(load?: DispatchLoad) { return load ? `${compactRun(load)} · ${runPeriod(load)} · ${finalDestination(load)}` : "—"; }
function routeTitle(load?: DispatchLoad) { return orderedStops(load).map(stop => `${stop.sequence}. ${cleanStopName(stop.name)}${stop.plannedArrivalUtc ? ` · ${localTime(stop.plannedArrivalUtc)}` : ""}`).join("\n"); }
function dayClass(day: number) { return day >= 5 ? "red" : day === 4 ? "amber" : "green"; }
function codeTitle(code?: string) { return code === "1" ? "Code 1 · can do anything" : code === "2" ? "Code 2 · established driver" : code === "3" ? "Code 3 · newer driver / straightforward work" : code === "4" ? "Code 4 · agency" : "Allocation code not set"; }
function routeMinutes(route: Record<string, unknown>) { const routes = route.routes as Array<{ summary?: { travelTimeInSeconds?: number } }> | undefined; const seconds = routes?.[0]?.summary?.travelTimeInSeconds; return typeof seconds === "number" && seconds > 0 ? Math.max(1, Math.ceil(seconds / 60)) : undefined; }
function statusLabel(status?: DispatchStatus) { return status === "Sent Awaiting Response" ? "Awaiting confirmation" : status === "Awaiting Dispatch" ? "Ready to dispatch" : status || "No Run"; }
function buildDriverText(load: DispatchLoad, dispatch: LoadDispatch, startTime?: string) { const lines = [`SLH ${load.southbound ? "Southbound " : ""}${load.reference}`, dispatch.driver ? `Driver: ${dispatch.driver.displayName}` : "", startTime ? `Planned start: ${startTime}` : "", dispatch.vehicle ? `Vehicle: ${dispatch.vehicle.registration}` : "", dispatch.trailer ? `Trailer: ${dispatch.trailer.trailerNumber}` : "", "", ...dispatch.stops.flatMap(stop => [`${stop.sequence}. ${stop.name}`, stop.address ? `Address: ${stop.address}` : "", stop.order?.reference ? `Ref: ${stop.order.reference}` : "", stop.order?.marketName ? `Market: ${stop.order.marketName}${stop.order.stallNumber ? ` · Stall ${stop.order.stallNumber}` : ""}` : "", stop.order?.driverInstructions ? `Notes: ${stop.order.driverInstructions}` : "", stop.order?.mapLink ? `Map: ${stop.order.mapLink}` : "", ""]), "Please reply to confirm receipt."]; return lines.filter((line, index, all) => line !== "" || (index > 0 && all[index - 1] !== "")).join("\n").trim(); }
function buildAmendmentText(load: DispatchLoad, dispatch: LoadDispatch, startTime?: string) { const lines = [`SLH AMENDMENT · ${load.southbound ? "Southbound " : ""}${load.reference}`, dispatch.driver ? `Driver: ${dispatch.driver.displayName}` : "", startTime ? `Revised planned start: ${startTime}` : "", dispatch.vehicle ? `Vehicle: ${dispatch.vehicle.registration}` : "", dispatch.trailer ? `Trailer: ${dispatch.trailer.trailerNumber}` : "", "", ...dispatch.stops.flatMap(stop => [`${stop.sequence}. ${stop.name}`, stop.address ? `Address: ${stop.address}` : "", stop.order?.reference ? `Ref: ${stop.order.reference}` : "", stop.order?.marketName ? `Market: ${stop.order.marketName}${stop.order.stallNumber ? ` · Stall ${stop.order.stallNumber}` : ""}` : "", stop.order?.driverInstructions ? `Notes: ${stop.order.driverInstructions}` : "", stop.order?.mapLink ? `Map: ${stop.order.mapLink}` : "", ""]), "Please reply to confirm the amendment."]; return lines.filter((line, index, all) => line !== "" || (index > 0 && all[index - 1] !== "")).join("\n").trim(); }
function statusClass(status?: DispatchStatus) { return status === "Confirmed" ? "confirmed" : status === "Sent Awaiting Response" ? "awaiting" : status === "Awaiting Dispatch" ? "ready" : "empty"; }

function TypeaheadSelect({ value, options, placeholder, onChange, disabled, listId }: { value: string; options: SearchOption[]; placeholder: string; onChange: (value: string) => void; disabled?: boolean; listId: string }) { const selected = options.find(option => option.id === value); const [text, setText] = useState(selected?.label || ""); const [open, setOpen] = useState(false); useEffect(() => setText(selected?.label || ""), [selected?.label, value]); const matches = useMemo(() => { const query = text.trim().toLowerCase(); return (query ? options.filter(option => `${option.label} ${option.search || ""}`.toLowerCase().includes(query)) : options).slice(0, 18); }, [options, text]); const choose = (option: SearchOption) => { setText(option.label); onChange(option.id); setOpen(false); }; return <div className="dispatch-typeahead"><input id={listId} role="combobox" aria-expanded={open} value={text} disabled={disabled} placeholder={placeholder} autoComplete="off" onFocus={() => setOpen(true)} onChange={event => { const next = event.target.value; setText(next); setOpen(true); if (!next.trim()) onChange(""); }} onBlur={() => window.setTimeout(() => setOpen(false), 120)} onKeyDown={event => { if (event.key === "Enter" && matches[0]) { event.preventDefault(); choose(matches[0]); } if (event.key === "Escape") { setOpen(false); setText(selected?.label || ""); } }} />{open && !disabled && <div className="dispatch-typeahead-menu">{matches.length === 0 ? <span className="dispatch-typeahead-empty">No matching option</span> : matches.map(option => <button type="button" key={option.id} className={option.id === value ? "selected" : ""} onMouseDown={event => event.preventDefault()} onClick={() => choose(option)}>{option.label}</button>)}</div>}</div>; }

export function DriverDispatch() {
  const token = useAccessToken(); const initialParams = useMemo(() => new URLSearchParams(window.location.search), []); const [date, setDate] = useState(initialParams.get("date") || today()); const [data, setData] = useState<Workbench>(); const [statuses, setStatuses] = useState<Record<string, DriverDispatchStatus>>({}); const [loading, setLoading] = useState(true); const [error, setError] = useState<string>(); const [statusError, setStatusError] = useState<string>(); const [filters, setFilters] = useState<Filters>(() => emptyFilters()); const [message, setMessage] = useState<MessageState>(); const [showDriverTools, setShowDriverTools] = useState(false); const [driverToolBusy, setDriverToolBusy] = useState(false); const [driverToolNotice, setDriverToolNotice] = useState<string>(); const [driverForm, setDriverForm] = useState<DriverForm>({ displayName: "", employeeNumber: "", driverType: "Agency", agencyName: "", startDate: date, days: 7 });
  const refresh = useCallback(async () => { setLoading(true); setError(undefined); setStatusError(undefined); try { const access = await token(); const workbench = await request<Workbench>(`/api/v1/driver-dispatch?date=${encodeURIComponent(date)}`, access, undefined, 90000); setData(workbench); try { const statusResponse = await request<{ planningDate: string; drivers: DriverDispatchStatus[] }>(`/api/v1/driver-dispatch-status?date=${encodeURIComponent(date)}`, access, undefined, 90000); setStatuses(Object.fromEntries(statusResponse.drivers.map(item => [item.driverId, item]))); } catch (statusException) { setStatuses({}); setStatusError(statusException instanceof Error ? statusException.message : "Driver message status and weekly-rest checks could not be loaded."); } } catch (exception) { setError(exception instanceof Error ? exception.message : "Driver Dispatch could not be loaded."); } finally { setLoading(false); } }, [date, token]); useEffect(() => { void refresh(); }, [refresh]); useEffect(() => { const params = new URLSearchParams(window.location.search); params.set("date", date); window.history.replaceState(null, "", `${window.location.pathname}?${params}`); }, [date]); useEffect(() => setDriverForm(current => ({ ...current, startDate: date })), [date]);
  const filteredDrivers = useMemo(() => { if (!data) return []; return data.drivers.filter(driver => { const assigned = data.loads.find(load => load.id === driver.assignedLoadId); const vehicle = assigned?.vehicleId ? data.vehicles.find(item => item.id === assigned.vehicleId) : undefined; const trailer = assigned?.trailerId ? data.trailers.find(item => item.id === assigned.trailerId) : undefined; const dispatchStatus = statuses[driver.driverId]?.dispatchStatus || (assigned ? "Awaiting Dispatch" : "No Run"); const values: Filters = { driver: `${driver.displayName} ${driver.employeeNumber}`, typeSkills: `${driver.driverType} ${driver.driverGroup || ""} ${driver.skills || ""} ${driver.agencyName || ""}`, code: driver.coding || "", day: String(driver.dayNumber), previous: `${driver.previousRunReference || ""} ${driver.previousFinalStop || ""} ${driver.previousRoute || ""}`, vehicle: `${vehicle?.registration || ""} ${driver.previousVehicleRegistration || ""} ${driver.suggestedVehicleRegistration || ""}`, trailer: `${trailer?.trailerNumber || ""} ${trailer?.type || ""}`, run: assigned ? `${suggestionRunLabel(assigned)} ${assigned.reference} ${assigned.rawReference} ${runSummary(assigned)}` : `${driver.suggestedRunReference || ""} unallocated`, assistant: `${driver.suggestion || ""} ${driver.previousFinalStop || ""} ${driver.assistantScore || ""}`, dispatch: `${dispatchStatus} ${statusLabel(dispatchStatus)}` }; return filterKeys.every(key => !filters[key].trim() || values[key].toLowerCase().includes(filters[key].trim().toLowerCase())); }); }, [data, filters, statuses]);
  async function syncDrivers() { setDriverToolBusy(true); setDriverToolNotice(undefined); try { await request(`/api/v1/driver-master/tachomaster/sync`, await token(), { method: "POST" }, 180000); setDriverToolNotice("Driver Master sync completed."); await refresh(); } catch (exception) { setDriverToolNotice(exception instanceof Error ? exception.message : "Driver Master sync failed."); } finally { setDriverToolBusy(false); } }
  async function addDriver() { if (!driverForm.displayName.trim()) { setDriverToolNotice("Enter the driver's name."); return; } if (driverForm.driverType === "Agency" && !driverForm.agencyName.trim()) { setDriverToolNotice("Enter the agency name."); return; } if (driverForm.driverType !== "Agency" && !driverForm.employeeNumber.trim()) { setDriverToolNotice("Enter the employee number, or use Sync Drivers first."); return; } setDriverToolBusy(true); setDriverToolNotice(undefined); try { const result = await request<{ message?: string }>(`/api/v1/driver-dispatch/drivers`, await token(), { method: "POST", body: JSON.stringify({ displayName: driverForm.displayName.trim(), employeeNumber: driverForm.employeeNumber.trim() || null, driverType: driverForm.driverType, agencyName: driverForm.driverType === "Agency" ? driverForm.agencyName.trim() : null, startDate: driverForm.startDate, days: driverForm.driverType === "Agency" ? driverForm.days : null }) }, 90000); setDriverToolNotice(result.message || "Driver saved."); setDriverForm(current => ({ ...current, displayName: "", employeeNumber: "" })); await refresh(); } catch (exception) { setDriverToolNotice(exception instanceof Error ? exception.message : "Driver could not be added."); } finally { setDriverToolBusy(false); } }
  return <section className="driver-dispatch-page"><div className="title-row dispatch-title"><div><p className="eyebrow">Planning → allocation → route → driver text</p><h1>Driver Dispatch</h1><p className="hint">Runs are kept compact so the driver list remains visible. Hover a run for its full route. Allocate opens the dispatch fields without clearing the run.</p></div><div className="title-actions dispatch-actions"><label>Planning date<input type="date" value={date} onChange={event => setDate(event.target.value)} /></label><button type="button" onClick={() => void syncDrivers()} disabled={driverToolBusy}>{driverToolBusy ? "Syncing…" : "Sync Drivers"}</button><button type="button" onClick={() => setShowDriverTools(value => !value)}>{showDriverTools ? "Close" : "Add Driver"}</button><button type="button" onClick={() => void refresh()} disabled={loading}>Refresh</button></div></div>{showDriverTools && <div className="dispatch-driver-tools"><div><strong>Add or roster a driver</strong><p className="hint">Agency drivers can be added directly; employed/casual drivers should normally come from TachoMaster/Sage HR.</p></div><label>Driver name<input value={driverForm.displayName} onChange={event => setDriverForm(current => ({ ...current, displayName: event.target.value }))} /></label><label>Type<select value={driverForm.driverType} onChange={event => setDriverForm(current => ({ ...current, driverType: event.target.value as DriverType }))}><option>Agency</option><option>Employed</option><option>Casual</option></select></label>{driverForm.driverType !== "Agency" && <label>Employee number<input value={driverForm.employeeNumber} onChange={event => setDriverForm(current => ({ ...current, employeeNumber: event.target.value }))} /></label>}{driverForm.driverType === "Agency" && <label>Agency<input value={driverForm.agencyName} onChange={event => setDriverForm(current => ({ ...current, agencyName: event.target.value }))} /></label>}<button className="primary" type="button" onClick={() => void addDriver()} disabled={driverToolBusy}>Save driver</button></div>}{driverToolNotice && <p className="notice inline-notice">{driverToolNotice}</p>}{error && <p className="notice inline-notice" style={{ borderColor: "#b42318" }}>{error}</p>}{statusError && <p className="notice inline-notice" style={{ borderColor: "#b7791f" }}>{statusError}</p>}{loading && !data && <div className="state">Building Driver Dispatch…</div>}{data && <><BuiltRunsQueue loads={data.loads} /><div className="dispatch-summary"><span><strong>{filteredDrivers.length}</strong> drivers shown</span><span><strong>{data.loads.length}</strong> planned runs</span><span><strong>{data.drivers.filter(driver => driver.suggestedRunId && !driver.assignedLoadId).length}</strong> assistant matches</span><span><strong>{data.drivers.filter(driver => driver.onLeave).length}</strong> away</span>{filterKeys.some(key => filters[key]) && <button type="button" className="text-button clear-dispatch-filters" onClick={() => setFilters(emptyFilters())}>Clear filters</button>}</div><div className="dispatch-table-wrap"><table className="dispatch-table"><thead><tr><th>Driver</th><th>Type / skills</th><th>Code</th><th>Day</th><th>Vehicle</th><th>Trailer</th><th>Run</th><th>Assistant</th><th>Status</th><th>Dispatch</th></tr><tr className="dispatch-filter-row">{filterKeys.map(key => <th key={key}><input aria-label={`Filter ${key}`} placeholder={filterPlaceholders[key]} value={filters[key]} onChange={event => setFilters(current => ({ ...current, [key]: event.target.value }))} /></th>)}</tr></thead><tbody>{filteredDrivers.map((driver, index) => <DispatchRow key={driver.driverId} driver={driver} data={data} status={statuses[driver.driverId]} showGroup={index === 0 || filteredDrivers[index - 1].driverType !== driver.driverType} token={token} refresh={refresh} openMessage={setMessage} />)}</tbody></table></div><p className="hint dispatch-footer-note">Run display = RUN X · AM/PM · final destination. Hover it for the complete route. Click Allocate to open the run, vehicle and trailer fields; Save allocation keeps the details and moves the run to Ready to dispatch. After the dispatch text is sent, status becomes Awaiting confirmation.</p></>}{message && <MessageDialog state={message} token={token} close={() => setMessage(undefined)} sent={async () => { setMessage(undefined); await refresh(); }} />}<p className="hint"><Link to="/planner">Back to Planner</Link></p></section>;
}

function BuiltRunsQueue({ loads }: { loads: DispatchLoad[] }) {
  const unallocated = [...loads].filter(load => !load.driverId).sort((left, right) => runNumber(left).localeCompare(runNumber(right), undefined, { numeric: true }));
  return <div data-testid="built-runs-queue" className="built-runs-compact"><div className="built-runs-compact-header"><strong>Runs ready to allocate</strong><span><strong>{unallocated.length}</strong> unallocated · {loads.length} built</span></div><div className="built-runs-compact-list">{unallocated.length === 0 ? <span className="hint">No unallocated runs.</span> : unallocated.map(load => <span key={load.id} className="built-run-chip" title={routeTitle(load)}><strong>{runSummary(load)}</strong></span>)}</div></div>;
}

function DispatchRow({ driver, data, status, showGroup, token, refresh, openMessage }: { driver: DispatchDriver; data: Workbench; status?: DriverDispatchStatus; showGroup: boolean; token: () => Promise<string>; refresh: () => Promise<void>; openMessage: (state: MessageState) => void }) {
  const initial = data.loads.find(load => load.id === driver.assignedLoadId); const suggestedLoad = driver.suggestedRunId ? data.loads.find(load => load.id === driver.suggestedRunId) : undefined; const suggestedTrailerId = suggestedLoad?.trailerId; const [loadId, setLoadId] = useState(initial?.id || ""); const [vehicleId, setVehicleId] = useState(initial?.vehicleId || driver.previousVehicleId || driver.suggestedVehicleId || ""); const [trailerId, setTrailerId] = useState(initial?.trailerId || suggestedTrailerId || ""); const [editingAllocation, setEditingAllocation] = useState(false); const [busy, setBusy] = useState(false); const [notice, setNotice] = useState<string>(); const selected = data.loads.find(load => load.id === loadId); const weeklyRestBlocked = status?.weeklyRestStatus === "Overdue" || status?.weeklyRestStatus === "Unverified";
  const vehicleOptions: SearchOption[] = [...data.vehicles].sort((left, right) => { const rank = (id: string) => id === driver.suggestedVehicleId ? 0 : id === driver.previousVehicleId ? 1 : 2; return rank(left.id) - rank(right.id) || left.registration.localeCompare(right.registration); }).map(vehicle => ({ id: vehicle.id, label: `${vehicle.registration}${vehicle.id === driver.suggestedVehicleId ? " · Assistant" : vehicle.id === driver.previousVehicleId ? " · yesterday" : ""}`, search: `${vehicle.registration} ${vehicle.fleetNumber || ""}` }));
  const trailerOptions: SearchOption[] = [...data.trailers].sort((left, right) => { const rank = (id: string) => id === suggestedTrailerId ? 0 : 1; return rank(left.id) - rank(right.id) || left.trailerNumber.localeCompare(right.trailerNumber); }).map(trailer => ({ id: trailer.id, label: `${trailer.trailerNumber}${trailer.id === suggestedTrailerId ? " · Assistant" : trailer.type ? ` · ${trailer.type}` : ""}`, search: `${trailer.trailerNumber} ${trailer.type || ""}` }));
  const runOptions: SearchOption[] = [...data.loads].filter(load => !load.driverId || load.id === driver.assignedLoadId).sort((left, right) => { const leftFree = left.driverId ? 1 : 0; const rightFree = right.driverId ? 1 : 0; if (leftFree !== rightFree) return leftFree - rightFree; if (left.id === driver.suggestedRunId && !left.driverId) return -1; if (right.id === driver.suggestedRunId && !right.driverId) return 1; return runNumber(left).localeCompare(runNumber(right), undefined, { numeric: true }); }).map(load => ({ id: load.id, label: `${runSummary(load)}${load.id === driver.assignedLoadId ? " · allocated" : ""}`, search: `${load.reference} ${load.rawReference} ${load.stops.map(stop => stop.name).join(" ")}` }));
  useEffect(() => { const load = data.loads.find(item => item.id === driver.assignedLoadId); setLoadId(load?.id || ""); setVehicleId(load?.vehicleId || driver.previousVehicleId || driver.suggestedVehicleId || ""); setTrailerId(load?.trailerId || (driver.suggestedRunId ? data.loads.find(item => item.id === driver.suggestedRunId)?.trailerId || "" : "")); setEditingAllocation(false); }, [data.loads, driver.assignedLoadId, driver.previousVehicleId, driver.suggestedRunId, driver.suggestedVehicleId]);
  function beginAllocation() { if (driver.onLeave || weeklyRestBlocked) return; if (!loadId && driver.suggestedRunId) setLoadId(driver.suggestedRunId); if (!vehicleId && driver.suggestedVehicleId) setVehicleId(driver.suggestedVehicleId); if (!trailerId && suggestedTrailerId) setTrailerId(suggestedTrailerId); setEditingAllocation(true); setNotice("Choose the run, vehicle and trailer, then Save allocation."); }
  function cancelAllocation() { const load = data.loads.find(item => item.id === driver.assignedLoadId); setLoadId(load?.id || ""); setVehicleId(load?.vehicleId || driver.previousVehicleId || driver.suggestedVehicleId || ""); setTrailerId(load?.trailerId || (driver.suggestedRunId ? data.loads.find(item => item.id === driver.suggestedRunId)?.trailerId || "" : "")); setEditingAllocation(false); setNotice(undefined); }
  function useSuggestion() { if (driver.onLeave || weeklyRestBlocked) return; if (driver.suggestedRunId) setLoadId(driver.suggestedRunId); if (driver.suggestedVehicleId) setVehicleId(driver.suggestedVehicleId); if (suggestedTrailerId) setTrailerId(suggestedTrailerId); setEditingAllocation(true); setNotice("Assistant suggestion loaded. Review the fields, then Save allocation."); }
  async function save() { if (driver.onLeave) return; if (weeklyRestBlocked) { setNotice(status?.weeklyRestMessage || "Weekly rest is due; this driver cannot be allocated."); return; } if (!loadId) { setNotice("Choose a run first."); return; } if (!vehicleId) { setNotice("Choose a vehicle before saving the allocation."); return; } setBusy(true); setNotice(undefined); try { const access = await token(); const previous = data.loads.find(load => load.id === driver.assignedLoadId); if (previous && previous.id !== loadId) await request(`/api/v1/runs/${encodeURIComponent(previous.id)}/allocation`, access, { method: "PUT", body: JSON.stringify({ driverId: null, vehicleId: previous.vehicleId || null, trailerId: previous.trailerId || null }) }); const saved = await request<DispatchLoad>(`/api/v1/runs/${encodeURIComponent(loadId)}/allocation`, access, { method: "PUT", body: JSON.stringify({ driverId: driver.driverId, vehicleId, trailerId: trailerId || null }) }); if (saved.driverId !== driver.driverId || saved.vehicleId !== vehicleId || (saved.trailerId || "") !== trailerId) throw new Error("The allocation write returned without the selected driver, vehicle or trailer. Refresh and try again."); setEditingAllocation(false); setNotice("Allocation saved. Ready to dispatch."); await refresh(); } catch (exception) { setNotice(exception instanceof Error ? exception.message : "Allocation could not be saved."); } finally { setBusy(false); } }
  async function prepareDispatch() { if (!loadId || !vehicleId) { setNotice("Choose a run and vehicle first."); return; } if (driver.onLeave) { setNotice("Driver is marked away in Sage HR."); return; } if (weeklyRestBlocked) { setNotice(status?.weeklyRestMessage || "Weekly rest is due; this driver cannot be dispatched."); return; } setBusy(true); setNotice(undefined); try { const access = await token(); await request(`/api/v1/runs/${encodeURIComponent(loadId)}/allocation`, access, { method: "PUT", body: JSON.stringify({ driverId: driver.driverId, vehicleId, trailerId: trailerId || null }) }); const route = await getDriverDispatchRoute(loadId, access); const minutes = routeMinutes(route); if (!minutes) throw new Error("The run could not be routed. The response did not contain a driving time."); let readiness = await request<DispatchReadiness>(`/api/v1/loads/${encodeURIComponent(loadId)}/dispatch-readiness`, access, { method: "POST", body: JSON.stringify({ routeDrivingMinutes: minutes, acknowledgeUnverified: false }) }, 90000); let acknowledged = false; if (!readiness.canDispatch && readiness.structuralReadiness?.classification === "Unverified" && readiness.structuralReadiness.requiresAcknowledgement) { const warnings = readiness.structuralReadiness.checks.filter(check => !check.passed).map(check => `• ${check.message}`).join("\n"); if (!window.confirm(`Pre-dispatch warnings:\n\n${warnings}\n\nAcknowledge and continue?`)) throw new Error("Dispatch cancelled; warnings were not acknowledged."); acknowledged = true; readiness = await request<DispatchReadiness>(`/api/v1/loads/${encodeURIComponent(loadId)}/dispatch-readiness`, access, { method: "POST", body: JSON.stringify({ routeDrivingMinutes: minutes, acknowledgeUnverified: true }) }, 90000); } if (!readiness.canDispatch) throw new Error(readiness.explanation || "Dispatch readiness did not pass."); const dispatch = await getRunDispatch(loadId, access); const latest = data.loads.find(load => load.id === loadId) || selected; if (!latest) throw new Error("Run details could not be loaded for the text preview."); const plannedStart = firstCollectionStop(latest)?.plannedArrivalUtc || latest.plannedStartUtc; openMessage({ load: latest, text: buildDriverText(latest, dispatch, localTime(plannedStart)), routeMinutes: minutes, acknowledgeUnverified: acknowledged, mode: "initial" }); } catch (exception) { setNotice(exception instanceof Error ? exception.message : "Dispatch could not be prepared."); } finally { setBusy(false); } }
  async function prepareAmendment() { if (!selected) { setNotice("Choose the allocated run before sending an amendment."); return; } if (driver.onLeave) { setNotice("Driver is marked away in Sage HR."); return; } setBusy(true); setNotice(undefined); try { const access = await token(); const dispatch = await getRunDispatch(selected.id, access); const plannedStart = firstCollectionStop(selected)?.plannedArrivalUtc || selected.plannedStartUtc; openMessage({ load: selected, text: buildAmendmentText(selected, dispatch, localTime(plannedStart)), routeMinutes: 0, acknowledgeUnverified: false, mode: "amendment" }); } catch (exception) { setNotice(exception instanceof Error ? exception.message : "Amendment preview could not be prepared."); } finally { setBusy(false); } }
  const persistedStatus = status?.dispatchStatus;
  const effectiveStatus: DispatchStatus = selected && persistedStatus === "No Run" ? "Awaiting Dispatch" : persistedStatus || (selected && driver.assignedLoadId === selected.id ? "Awaiting Dispatch" : "No Run");
  const canEditAllocation = effectiveStatus !== "Sent Awaiting Response" && effectiveStatus !== "Confirmed";
  const currentVehicle = data.vehicles.find(vehicle => vehicle.id === (initial?.vehicleId || vehicleId));
  const currentTrailer = data.trailers.find(trailer => trailer.id === (initial?.trailerId || trailerId));
  const displayLoad = initial || selected || suggestedLoad;
  return <>{showGroup && <tr className="dispatch-group"><td colSpan={10}>{driver.driverType === "Agency" ? "AGENCY" : driver.driverType === "Casual" ? "CASUAL" : "EMPLOYED"}</td></tr>}<tr className={`${weeklyRestBlocked ? "weekly-rest-blocked" : driver.onLeave ? "on-leave" : ""} ${editingAllocation ? "allocation-editing" : ""}`}><td><strong>{driver.displayName}</strong><small>{driver.employeeNumber}</small>{driver.onLeave && <em>{driver.leaveType || "Sage HR leave"}</em>}</td><td><div className="badge-line"><span className={`driver-type type-${driver.driverType.toLowerCase()}`} title={driver.agencyName || driver.driverType}>{driver.driverType === "Agency" ? "A" : driver.driverType === "Casual" ? "C" : "E"}</span>{(driver.skills || "").split(/[,;|/]+/).map(skill => skill.trim()).filter(Boolean).slice(0, 3).map(skill => <span className="skill-badge" key={skill}>{skill}</span>)}</div><small>{driver.driverType === "Agency" ? driver.agencyName || "Agency" : driver.driverGroup || ""}</small></td><td><span className={`code-badge code-${driver.coding || "x"}`} title={codeTitle(driver.coding)}>{driver.coding || "—"}</span></td><td><span className={`day-bubble ${dayClass(driver.dayNumber)}`} title={`Day ${driver.dayNumber}`}>{driver.dayNumber}</span></td><td>{editingAllocation ? <TypeaheadSelect disabled={driver.onLeave || weeklyRestBlocked} value={vehicleId} onChange={setVehicleId} options={vehicleOptions} placeholder="Vehicle…" listId={`vehicle-${driver.driverId}`} /> : <div className="allocation-readonly"><strong>{currentVehicle?.registration || driver.suggestedVehicleRegistration || "—"}</strong>{driver.previousVehicleRegistration && <small>Prev: {driver.previousVehicleRegistration}</small>}</div>}</td><td>{editingAllocation ? <TypeaheadSelect disabled={driver.onLeave || weeklyRestBlocked} value={trailerId} onChange={setTrailerId} options={trailerOptions} placeholder="Trailer…" listId={`trailer-${driver.driverId}`} /> : <div className="allocation-readonly"><strong>{currentTrailer?.trailerNumber || "—"}</strong></div>}</td><td><div className="run-cell">{editingAllocation ? <TypeaheadSelect disabled={driver.onLeave || weeklyRestBlocked} value={loadId} onChange={setLoadId} options={runOptions} placeholder="Run…" listId={`run-${driver.driverId}`} /> : displayLoad ? <RunHover load={displayLoad} /> : <span className="hint">No run</span>}{editingAllocation && selected && <RunHover load={selected} />}</div></td><td className="assistant-cell"><span>{driver.suggestion || (driver.previousFinalStop ? `Yesterday finished ${driver.previousFinalStop}.` : "Available for allocation.")}</span>{driver.assistantScore != null && driver.assistantScore > 0 && <small>Match {driver.assistantScore}</small>}{!driver.onLeave && !weeklyRestBlocked && (driver.suggestedRunId || driver.suggestedVehicleId) && <button className="assistant-use" type="button" onClick={useSuggestion}>Use suggestion</button>}</td><td className="dispatch-status-cell"><span className={`dispatch-status-pill ${statusClass(effectiveStatus)}`}>{statusLabel(effectiveStatus)}</span>{editingAllocation && <small>Allocation fields open</small>}{status?.weeklyRestStatus && status.weeklyRestStatus !== "Ready" && status.weeklyRestStatus !== "Unknown" && <small title={status.weeklyRestMessage}>Tacho: {status.weeklyRestStatus === "Overdue" ? "Weekly rest due" : status.weeklyRestStatus === "Unverified" ? "Weekly rest unavailable" : "Rest due soon"}</small>}{status?.lastDriverReply && effectiveStatus === "Confirmed" && <small title={status.lastDriverReply}>{status.lastDriverReply.length > 72 ? `${status.lastDriverReply.slice(0, 72)}…` : status.lastDriverReply}</small>}</td><td><div className="dispatch-buttons">{editingAllocation ? <><button className="primary" type="button" onClick={() => void save()} disabled={busy || driver.onLeave || weeklyRestBlocked || !loadId || !vehicleId}>{busy ? "Saving…" : "Save allocation"}</button><button type="button" onClick={cancelAllocation} disabled={busy}>Cancel</button></> : <>{selected && driver.assignedLoadId === selected.id && effectiveStatus === "Awaiting Dispatch" && <button className="primary" type="button" onClick={() => void prepareDispatch()} disabled={busy || driver.onLeave || weeklyRestBlocked}>{busy ? "Preparing…" : "Dispatch"}</button>}{selected && (effectiveStatus === "Sent Awaiting Response" || effectiveStatus === "Confirmed") && <button type="button" onClick={() => void prepareAmendment()} disabled={busy || driver.onLeave}>{busy ? "Working…" : "Amendment"}</button>}{!weeklyRestBlocked && canEditAllocation && <button type="button" onClick={beginAllocation} disabled={busy || driver.onLeave}>{driver.assignedLoadId ? "Edit allocation" : "Allocate"}</button>}</>}{weeklyRestBlocked && <span className="dispatch-blocked-note">{status?.weeklyRestStatus === "Overdue" ? "Weekly rest due" : "Tacho check unavailable"}</span>}</div>{notice && <small className="row-notice">{notice}</small>}</td></tr></>;
}

function RunHover({ load }: { load: DispatchLoad }) { const first = firstCollectionStop(load); const direction = runDirection(load); return <span className="run-hover" tabIndex={0} title={routeTitle(load)}><b>{runSummary(load)}</b><span className="run-popover"><strong>{runSummary(load)}</strong><small>{direction}{first?.name ? ` · First collection: ${cleanStopName(first.name)}` : ""}</small><small>{load.palletSpacesUsed ?? "—"}{load.totalPalletSpaces ? ` / ${load.totalPalletSpaces}` : ""} {load.capacityType || "load units"}</small>{orderedStops(load).map(stop => <span key={stop.id}>{stop.sequence}. {cleanStopName(stop.name)}{stop.plannedArrivalUtc ? ` · ${localTime(stop.plannedArrivalUtc)}` : ""}{stop.address ? ` · ${stop.address}` : ""}</span>)}{load.plannerNotes && <em>{load.plannerNotes}</em>}</span></span>; }
function MessageDialog({ state, token, close, sent }: { state: MessageState; token: () => Promise<string>; close: () => void; sent: () => Promise<void> }) { const [text, setText] = useState(state.text); const [busy, setBusy] = useState(false); const [error, setError] = useState<string>(); async function send() { setBusy(true); setError(undefined); try { await request(`/api/v1/loads/${encodeURIComponent(state.load.id)}/driver-message/sms`, await token(), { method: "POST", body: JSON.stringify({ message: text, dispatch: state.mode === "initial", routeDrivingMinutes: state.mode === "initial" ? state.routeMinutes : null, acknowledgeUnverified: state.mode === "initial" ? state.acknowledgeUnverified : false }) }, 90000); await sent(); } catch (exception) { setError(exception instanceof Error ? exception.message : "Driver text could not be sent."); } finally { setBusy(false); } } return <div className="dispatch-modal-backdrop" role="dialog" aria-modal="true"><div className="dispatch-modal"><div className="title-row"><div><p className="eyebrow">{state.mode === "initial" ? "Dispatch text preview" : "Amendment text preview"}</p><h2>{runSummary(state.load)}</h2><p className="hint">Review the exact SMS below before sending. You can edit it.</p></div><button type="button" onClick={close} disabled={busy}>Close</button></div><textarea rows={16} value={text} onChange={event => setText(event.target.value)} />{error && <p className="notice inline-notice" style={{ borderColor: "#b42318" }}>{error}</p>}<div className="dispatch-modal-actions"><button type="button" onClick={close} disabled={busy}>Cancel</button><button className="primary" type="button" onClick={() => void send()} disabled={busy || !text.trim()}>{busy ? "Sending…" : state.mode === "initial" ? "SEND" : "SEND AMENDMENT"}</button></div></div></div>; }
